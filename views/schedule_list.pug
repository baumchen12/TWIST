extends layout

block content
  h1= title
  
  ul
    each schedule in schedule_list
      li 
        a(href=schedule.url) #{schedule.title} 
        //|  (#{book.author.name})

    else
      li There are no schedules.