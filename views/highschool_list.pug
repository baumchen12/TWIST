extends layout

block content
  h1= title
  
  ul
    each highschool in highschool_list
      li 
        a(href=highschool.url) #{highschool.name} 
        //|  (#{book.author.name})

    else
      li There are no highschools.