extends layout

block content
  h1= title
  p Welcome to #[em LocalLibrary], a very basic Express website developed as a tutorial example on the Mozilla Developer Network.

  h1 Dynamic content

  if error
    p Error getting dynamic content.
  else
    p The library has the following record counts:

    ul
      li #[strong Participants:] !{data.participant_count}
      li #[strong Presenters:] !{data.presenter_count}
      li #[strong Sessions:] !{data.session_count}
      li #[strong Topics:] !{data.topic_count}
      li #[strong Rooms:] !{data.room_count}
      li #[strong Schedules:] !{data.schedule_count}
      li #[strong High Schools:] !{data.highschool_count}